[build]
builder = "nixpacks"
buildCommand = "cd backend && pip install -r requirements.txt"

[deploy]
startCommand = "cd backend && python manage.py migrate --settings=config.settings.production && python manage.py runserver 0.0.0.0:$PORT --settings=config.settings.production"